Fix: Ensure public directory and all assets are tracked and pushed

- Staged all untracked files, including the `public/` directory and its contents.
- Reverted image paths in `Header.astro` and `index.astro` to simple root-relative paths.
- Confirmed `publicDir: './public'` in `astro.config.mjs`.